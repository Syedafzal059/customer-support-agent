{
  "name": "ai-support-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n watch,srv -c blue,magenta \"vite build --watch\" \"wait-on dist/index.html && serve -s dist --listen tcp://127.0.0.1:5173 --no-port-switching\"",
    "build": "vite build",
    "preview": "npm run build && serve -s dist --listen tcp://127.0.0.1:5173 --no-port-switching",
    "start": "npm run dev"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "concurrently": "^9.1.0",
    "serve": "^14.2.4",
    "vite": "^5.4.11",
    "wait-on": "^8.0.1"
  }
}
